# Titre de l'application
VITE_APP_TITLE=Waste Management System

# URL de l'API backend (optionnel). Si absent, les données sont stockées en localStorage.
# Exemple avec l'API locale :
# VITE_API_URL=http://localhost:3001
